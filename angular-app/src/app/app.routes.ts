import { Routes } from '@angular/router';
import { CounterLoaderComponent } from './counter-loader/counter-loader.component';

export const routes: Routes = [
    {
        path: 'counter',
        component: CounterLoaderComponent
    }
];
